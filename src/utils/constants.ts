export const METADATA_REFRESH_MS = 60 * 60 * 1000; // hourly
export const LIGHTER_REFRESH_MS = 5 * 60 * 1000; // every 5 minutes
export const EDGEX_CYCLE_MS = 60 * 60 * 1000; // hourly
export const EDGEX_VISIBLE_REFRESH_MS = 5 * 60 * 1000; // refresh visible contracts every 5 minutes
export const VISIBLE_MIN_REFRESH_MS = 30 * 1000; // minimum gap between visible contract refreshes
export const EDGEX_TOP_REFRESH_MS = 10 * 60 * 1000; // refresh full set every 10 minutes
export const FETCH_GAP_MS = 500; // avoid rate limiting between sequential contract requests
export const DISPLAY_LIMIT = 10;
